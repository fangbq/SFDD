#ifndef DEFINE_H
#define DEFINE_H

#include <iostream>
#include <fstream>
#include <sstream>
#include <string>
#include <vector>
#include <stack>
#include <map>
#include <set>
#include <cassert>

using namespace std;


enum OPERATOR_TYPE {
    XOR,
    CONJUNCTION,
    DISCONJUNCTION
};

enum VTREE_TYPE {
    TRIVIAL_TREE,
    RANDOM_TREE
};


#endif
